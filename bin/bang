#!/usr/bin/env bash

URL="https://www.duckduckgo.com/?q="
BANGS=$(cat <<EOS
!amjp
!archwiki
!archpkg
!aur
!bang
!deepl
!github
!rust
!slant
!translate
!vimawesome
EOS
)

bang=$( (echo "$BANGS") | fmenu )

if [[ -n "$bang" ]]; then
  query=$(readp -p "$bang:")
  if [[ -v BROWSER ]]; then
    $BROWSER "$URL $bang $query"
  else
    xdg-open "$URL $bang $query"
  fi
fi
