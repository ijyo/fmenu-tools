#!/usr/bin/env bash

URL="https://www.duckduckgo.com/?q="

query=$(readp -p DuckDuckGo:)

if [[ -n "$query" ]]; then
  if [[ -v BROWSER ]]; then
    $BROWSER "$URL$query"
  else
    xdg-open "$URL$query"
  fi
fi
