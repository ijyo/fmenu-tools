#!/usr/bin/env bash

URL="https://www.oxfordlearnersdictionaries.com/definition/american_english/"

query=$(readp -p oxford:)

if [[ -n "$query" ]]; then
  if [[ -x $(which vimb) && false ]]; then
    vimb "$URL$query"
  elif [[ -v BROWSER ]]; then
    $BROWSER "$URL$query"
  else
    xdg-open "$URL$query"
  fi
fi
