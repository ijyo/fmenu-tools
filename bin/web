#!/usr/bin/env bash

URL="https://www.startpage.com/do/dsearch?query="

query=$(readp -p Startpage:)

if [[ -n "$query" ]]; then
  if [[ -v BROWSER ]]; then
    $BROWSER "$URL$query"
  else
    xdg-open "$URL$query"
  fi
fi
